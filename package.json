{
  "name": "tracScraper",
  "description": "A Node.js / Express app that creates scrapes data from a Trac issue tracking website",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "npm run test-all",
    "test-all": "mocha -R spec --recursive",
    "test-all-coverage": "istanbul cover node_modules/mocha/bin/_mocha -- -R spec --recursive"
  },
  "dependencies": {
    "express": "*",
    "request": "*",
    "winston": "~0.9.0",
    "nconf": "~0.7.1",
    "cheerio": "~0.19.0"
  },
  "devDependencies": {
    "mocha": "*",
    "supertest": "*",
    "chai": "~2.1.0",
    "proxyquire": "~1.3.1",
    "sinon": "~1.12.2",
    "sinon-chai": "~2.7.0",
    "nock": "~1.1.0"
  }
}
